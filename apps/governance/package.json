{
  "name": "@eadwyn/governance",
  "version": "0.1.0",
  "private": true,
  "description": "Governance service: the review-merges layer. Lists pending merges, records reviewer decisions, approves or rejects candidates by quorum.",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsup",
    "start": "node dist/index.js",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@eadwyn/federation-sdk": "workspace:*",
    "@eadwyn/service-kit": "workspace:*",
    "@eadwyn/shared-protocol": "workspace:*",
    "hono": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsup": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
